# Python
__pycache__/
.venv/

# All local configs
.*.local

# Test Coverage
.coverage
/htmlcov/

# LaTeX build artifacts
docs/fuzz.sty
docs/*.mf
docs/*.pk
docs/*.tfm
docs/*.aux
docs/*.log
docs/*.fuzz
docs/*.toc
docs/*.fdb_latexmk
docs/*.fls
docs/*.out
docs/*.pdf
# Human-facing rendered spec (PDFs for people, source for tools) — tracked, linked from public writing
!docs/display_lifecycle.pdf
docs/*.synctex.gz

# IDEs
.vscode/
.idea/

# Claude
.claude/settings.local.json
.claude/projects/
.claude/worktrees/
.claude/scheduled_tasks*
.mcp.json
.codex/config.toml
.tmp/*
!.tmp/.gitkeep
.worktrees/

# Ethos mission log
.ethos/

# Vox tool local data
.vox/

# Punt Labs toolchain ignores
.punt-labs/quarry/captures/*.md
.punt-labs/local/
*.ini
uv.toml

# Spike/scratch/demo trees — release tooling requires a clean working tree
# (CLAUDE.md gotcha from v0.5.0: punt release fails if demos/ is dirty)
spikes/*
!spikes/io_model_v1/
demos/

# bd create lock (transient)
.create.lock

# Per-user plugin local config (vox/biff/etc.) — never tracked
*.local.md

# LaTeX build artifacts for root prfaq.tex (PDF is committed for the README badge link)
/prfaq.aux
/prfaq.bbl
/prfaq.bcf
/prfaq.blg
/prfaq.log
/prfaq.out
/prfaq.run.xml
/prfaq.toc
/prfaq.fdb_latexmk
/prfaq.fls
