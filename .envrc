use keychain openai-api-key OPENAI_API_KEY
use keychain elevenlabs-api-key ELEVENLABS_API_KEY
export GITHUB_PERSONAL_ACCESS_TOKEN="$(gh auth token -h github.com)"
export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
export TMPDIR="$PWD/.tmp"
mkdir -p "$TMPDIR"
